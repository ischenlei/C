#include <stdio.h>
#include <stdlib.h>
//编写一段程序，输入整数后循环显示1234567890，显示的位数和输入的整数值相同
int main()
{
    int num, i,no;

    printf("请输入一个整数:");
    scanf("%d", &num);

    for(i = 0; i <= num; ++i)
    {
        no =

    }

    return 0;
}
