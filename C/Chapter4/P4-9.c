#include <stdio.h>
#include <stdlib.h>

int main()
{
    int no,i;

    printf("请输入一个正整数:");
    scanf("%d",&no);

    i = 2;

    if(i <= no)
    else if(no % 2 == 0)
            printf("%d ", i++);

    return 0;
}
